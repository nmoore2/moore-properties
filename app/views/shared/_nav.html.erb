<!-- nav -->
<nav class="navbar navbar-default">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar top-bar"></span>
            <span class="icon-bar middle-bar"></span>
            <span class="icon-bar bottom-bar"></span>
            </button>
            <a class="navbar-brand" href="/">
              <%= image_tag 'moore-prop-logo.jpg', class: 'img-responsive logo' %>
            </a>
        </div><!-- / navbar-header -->
        <div id="navbar-collapse" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li class="<%= 'active' if action_name == 'index' %>"><a href="/"><span>HOME</span></a></li>
                <li class="<%= 'active' if action_name == 'faq' %>"><a href="<%= faq_page_path %>"><span>FAQ</span></a></liclass>
                <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span>RENTALS</span> <span class="dropdown-icon"></span></a>
                <ul class="dropdown-menu animated zoomIn fast">
                  <% @properties.each do |property| %>
                    <li class="<%= 'active' if @property == property %>"><a href="<%= property_path(property) %>"><span><%= property.title %></span></a></li>
                  <% end %>
                </ul>
                </li>
                <li class="<%= 'active' if action_name == 'contact' %>"><a href="<%= contact_path %>"><span>CONTACT</span></a></li>
                <li class="<%= 'active' if action_name == 'apply' %>"><a href="<%= apply_path %>" class="btn btn-primary-filled apply"><span> APPLY</span></a></li>
                <% if current_user.present? %>
                  <li><a href="<%= admin_path %>">ADMIN</a></li>
                <% end %>
            </ul>
        </div><!--/ nav-collapse -->
    </div><!-- / container -->
</nav>
<!-- / nav -->

$(function() {
$("#new_contact").validator().on("submit", function (event) {
    if (event.isDefaultPrevented()) {
        // handle the invalid form...
        formError();
        submitMSG(false, "Please fill all required fields!");
    } else {
        // everything looks good!
        // event.preventDefault();
        // submitForm();
    }
});
//
//
// function submitForm(){
//
//     // Initiate Variables With Form Content
//     var name = $("#contact_name").val();
//     var email = $("#contact_email").val();
//     // var msg_subject = $("#contact_message").val();
//     var message = $("#contact_message").val();
//
//
//     $.ajax({
//         type: "POST",
//         url: '/contact',
//         data: $(this).serialize(),
//         dataType: 'json',
//         success : function(text){
//             if (text == "success"){
//                 formSuccess();
//
//             } else {
//               console.log(text);
//                 formError();
//                 submitMSG(false,text);
//             }
//         }
//     });
// }

function formSuccess(){
    $("#new_contact");
    submitMSG(true, "Message Sent!")
}

function formError(){
    $("#new_contact").removeClass().addClass('animated').one('webkitAnimationEnd mozAnimationEnd MSAnimationEnd oanimationend animationend', function(){
        $(this).removeClass();
    });
}

function submitMSG(valid, msg){
    if(valid){
        var msgClasses = "h3 text-center fadeIn animated text-success";
    } else {
        var msgClasses = "h3 text-center text-danger";
    }
    $("#msgSubmit").removeClass().addClass(msgClasses).text(msg);
}

})
